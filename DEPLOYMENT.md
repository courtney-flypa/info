# GitHub Pages 部署指南 🌸

## 準備工作

### 1. 創建 GitHub 倉庫
1. 前往 GitHub 創建新倉庫
2. 將本地代碼推送到 GitHub

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/你的用戶名/你的倉庫名.git
git push -u origin main
```

### 2. 配置 GitHub Pages
1. 前往你的倉庫設置頁面
2. 點擊左側的 **"Pages"**
3. 在 **"Build and deployment"** 部分：
   - **Source**: 選擇 `GitHub Actions`

### 3. 配置 basePath（如果需要）

如果你的倉庫名稱不是 `username.github.io`，需要設置 basePath：

1. 打開 `next.config.ts`
2. 取消註釋並修改這一行：
   ```typescript
   basePath: '/你的倉庫名',
   ```

例如，如果倉庫是 `https://github.com/courtney/info`，則設置：
```typescript
basePath: '/info',
```

**注意**：如果倉庫名是 `username.github.io`，則不需要設置 basePath。

## 部署步驟

### 方法一：自動部署（推薦）✨

每次推送到 `main` 分支時，GitHub Actions 會自動構建和部署：

```bash
git add .
git commit -m "Update site"
git push
```

### 方法二：本地構建後推送

```bash
# 構建靜態文件
npm run build

# 推送到 GitHub
git add .
git commit -m "Update site"
git push
```

## 查看部署狀態

1. 前往倉庫的 **"Actions"** 標籤
2. 查看最新的工作流運行狀態
3. 部署成功後，網站會在幾分鐘內可用

## 訪問網站

- 如果倉庫名是 `username.github.io`：
  - 網址：`https://username.github.io`

- 如果倉庫名是其他名稱（例如 `info`）：
  - 網址：`https://username.github.io/info`

## 常見問題

### 1. 頁面顯示 404
- 確認 GitHub Pages 設置正確
- 確認 `next.config.ts` 中的 `basePath` 設置正確
- 等待幾分鐘讓部署生效

### 2. 樣式或圖片無法加載
- 檢查 `basePath` 設置
- 確認 `images.unoptimized` 設置為 `true`

### 3. 路由不工作
- GitHub Pages 只支持靜態網站
- 確保所有路由在構建時都有對應的 HTML 文件

### 4. 更新沒有生效
- 清除瀏覽器緩存
- 檢查 GitHub Actions 是否成功運行
- 等待幾分鐘讓 CDN 更新

## 本地測試靜態導出

在部署前，可以本地測試靜態導出：

```bash
# 構建
npm run build

# 安裝 serve（如果還沒安裝）
npm install -g serve

# 運行靜態文件
serve out
```

然後訪問 `http://localhost:3000` 查看效果。

## 注意事項

⚠️ **GitHub Pages 限制**：
- 靜態網站專用，不支持服務器端渲染 (SSR)
- 不支持 API routes
- 不支持動態路由的服務器端功能
- 倉庫大小建議不超過 1GB
- 月帶寬限制約 100GB

💡 **本專案特性**：
- ✅ 使用客戶端渲染
- ✅ 資料存儲在瀏覽器 localStorage
- ✅ 完全靜態，適合 GitHub Pages

## 更新網站

修改代碼後：

```bash
git add .
git commit -m "描述你的更改"
git push
```

GitHub Actions 會自動重新構建和部署網站。

---

祝你部署順利！🌸✨
如有問題，請查看 GitHub Actions 的運行日誌。

